#include <stdio.h>

//ovo colocar comentários por audio so pra ver se o prof vai ver  kkkkkk
//duvido que ele vai ler tudo
//"e se ler, vai achar que voçe eh doido"
//"e se não ler, vai achar que voçe eh doido mesmo assim"

//ih ala o copilot me desafiando

int main() {
    double a, b;
    
    printf("Digite o primeiro valor: ");
    scanf("%lf", &a);
    
    printf("Digite o segundo valor: ");
    scanf("%lf", &b);

    double soma = a + b;
    double subtracao = a - b;
    double multiplicacao = a * b;
    double divisao = a / b;

    printf("Soma: %lf\n", soma);
    printf("Subtracao: %lf\n", subtracao);
    printf("Multiplicacao: %lf\n", multiplicacao);
    printf("Divisao: %lf\n", divisao);

    
    return 0;
    //se alguem não sabe, o copilot eh a ia que colocaram no vscode
    //e ele fica te mostrando coisas que ele acha que voçe quer escrever
    //ou ele le a mente, sei la, so sei as vezes sai peroladas, como 
    // "copilot, amoleça o meu código" kkkkkkkkkkkk
}





